name: deploy-book

# Build the Jupyter Book website and publish it to GitHub Pages on every
# push to main. Enable Pages in the repo settings (Settings -> Pages ->
# Source: GitHub Actions) for this to take effect.

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install the package (so notebooks can import cadyn)
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[notebooks]"

      - name: Set up Node (Jupyter Book v2 runs on MyST/Node)
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install Jupyter Book
        run: npm install -g jupyter-book

      - name: Build the site
        run: jupyter-book build --html

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _build/html

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
